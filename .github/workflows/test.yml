on: [push]

jobs:
  local-php-security-checker:
    runs-on: ubuntu-latest
    name: checking
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Security Check action step
        uses: ./
        id: local-php-security-checker
        with:
          lockFile: ./.github/workflows/composer.lock